{
  "name": "extract-html-classes",
  "displayName": "Extract HTML Classes",
  "version": "0.0.1",
  "publisher": "yourname",
  "activationEvents": [
    "onCommand:extension.extractHtmlClassesToClipboard"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.extractHtmlClassesToClipboard",
        "title": "Extract CSS Classes to Clipboard"
      }
    ]
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^3.6.2",
    "typescript": "^5.0.0"
  }
}
